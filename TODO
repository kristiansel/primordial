in Parser::parseSimpleObj :
	Fix texture coordinate seams

in Master::handleInput :
	Add mouselook

General :
	Skeletal animation: Transform animation channels 
	by "armature matrix" (first matrix to hit a bone
	basically) Save the matrix, decomponse into tra,
	rot and scale, and apply it to every key frame
	
	After that-> interpolation -> change shading to include
	animation!
	
	Then clean up, make a resource buffer for skeletons etc.

